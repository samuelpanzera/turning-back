# Ambiente de Produção - PostgreSQL (Heroku)
ENV=production
PORT=${PORT}
APP_NAME=turning-back

# Database - PostgreSQL (Produção)
DB_TYPE=postgres
# As variáveis do Heroku serão injetadas automaticamente
# DATABASE_URL será parseada pelo Heroku

# Logging
LOG_LEVEL=info
LOG_FORMAT=json